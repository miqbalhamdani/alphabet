{
  "name": "alphabet",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "deploy": "wrangler pages deploy .",
    "deploy:project": "wrangler pages deploy . --project-name $CF_PAGES_PROJECT",
    "deploy:alphabet": "wrangler pages deploy . --project-name alphabet"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
